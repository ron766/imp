FTP PORTAL

- login ->
	Raki@8145
	rakesh.boddu@raweng.com

- clone/pull git

- npm install

- download mongo dump

- $ cd stagingCustomWebTool/ -->

	$ mongorestore -d dev-customapp audits.bson

	$ mongorestore -d dev-customapp users.bson

	$ mongo
		- use dev-customapp


- to run -->
	
	- run redis -> sudo systemctl start redis

	- NODE_ENV=localdev nodemon app

	- if <shapshot issue> --> config set stop-writes-on-bgsave-error no 
	
	- ~/Codes/react/practice/tibco-docs-ftp-portal/public/ftp_portal/manage_portal$ gulp gulp-build













